import { createTheme } from "@mui/material";

export const theme = createTheme({
  palle: {
    primary: {
      main: "#1760a5",
      light: "skyblue",
    },
    secondary: {
      main: "#15c630",
    },
    otherColor: {
      main: "#999",
      mycolor: "#000",
    },
  },
});